import 'package:flutter_test/flutter_test.dart';

import 'package:scanner_app/main.dart';

void main() {
  test('scanner app can be created', () {
    expect(const MiitStoreScannerApp(), isA<MiitStoreScannerApp>());
  });
}
