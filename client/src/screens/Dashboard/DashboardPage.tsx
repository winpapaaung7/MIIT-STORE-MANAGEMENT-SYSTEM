/* eslint-disable @typescript-eslint/no-explicit-any */
import { useCallback, useEffect, useRef, useState } from "react";
import { Bar, BarChart, Cell, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { AlertTriangle, Boxes, CheckCircle2, ChevronLeft, ChevronRight, ClipboardCheck, type LucideIcon } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
const API=import.meta.env.VITE_API_BASE_URL??"http://localhost:5000"; const C={available:"#16a34a",inUse:"#f97316",damagedMaintenance:"#dc2626"}; const n=(v:any)=>typeof v==="number"&&Number.isFinite(v)?v:0;
export default function DashboardPage(){
 const [data,setData]=useState<any>(null),[loading,setLoading]=useState(true),[filters,setFilters]=useState({academicYearId:"",departmentId:"",categoryId:"",status:"",departmentItemsPage:1}),[hover,setHover]=useState<string|null>(null);const request=useRef(0);
 const load=useCallback(async()=>{const q=new URLSearchParams({departmentItemsPage:String(filters.departmentItemsPage),departmentItemsLimit:"8",recentItemsLimit:"5"});Object.entries(filters).forEach(([k,v])=>{if(v&&k!=="departmentItemsPage")q.set(k,String(v))});if(hover)q.set("activeDepartmentId",hover);const id=++request.current;setLoading(true);try{const r=await fetch(API+"/api/dashboard/overview?"+q);const p=await r.json();if(id===request.current)setData(p)}finally{if(id===request.current)setLoading(false)}},[filters,hover]);useEffect(()=>{void load()},[load]);
 const change=(key:string,value:string)=>{setFilters(x=>({...x,[key]:value}))};const choose=(id:any)=>{const value=String(id??"");if(value&&value!==hover){setHover(value);setFilters(x=>({...x,departmentItemsPage:1}))}};
 const s=data?.summary,dept=(data?.departmentOverview??[]).map((x:any)=>({...x,available:n(x.available),inUse:n(x.inUse),damagedMaintenance:n(x.damagedMaintenance)})).sort((a:any,b:any)=>a.departmentName==="Store"?-1:b.departmentName==="Store"?1:String(a.departmentName).localeCompare(String(b.departmentName)));const cards=[["Total Items",n(s?.totalItems),"All tracked units",Boxes,"text-blue-600","bg-blue-50",100],["Available",n(s?.available),"Ready for use",CheckCircle2,"text-green-600","bg-green-50",n(s?.availablePercentage)],["In Use",n(s?.inUse),"Issued units",ClipboardCheck,"text-orange-600","bg-orange-50",n(s?.inUsePercentage)],["Damaged / Maintenance",n(s?.damagedMaintenance),"Needs attention",AlertTriangle,"text-red-600","bg-red-50",n(s?.damagedMaintenancePercentage)]] as [string,number,string,LucideIcon,string,string,number][];
 return <div className="space-y-6 pb-4"><header className="flex flex-col gap-4 xl:flex-row xl:items-end xl:justify-between"><div><h1 className="text-3xl font-bold text-slate-950">Dashboard</h1></div><div className="grid w-full grid-cols-1 gap-2 sm:grid-cols-2 xl:w-auto xl:grid-cols-4">{[["Academic Year","academicYearId","academicYears"],["Department","departmentId","departments"],["Category","categoryId","categories"],["Status","status","statuses"]].map(([label,key,source])=><select key={key} aria-label={label} value={(filters as any)[key]} onChange={e=>change(key,e.target.value)} className="h-10 min-w-[160px] rounded-lg border border-slate-200 bg-white px-3 text-sm"><option value="">All {label}s</option>{(data?.filterOptions?.[source]??[]).map((o:any)=><option key={o.id??o.name??o} value={String(o.id??o.name??o)}>{o.name??o}</option>)}</select>)}</div></header>
 <section className="grid gap-4 sm:grid-cols-2 md:grid-cols-3 xl:grid-cols-5">{loading&&!data?Array.from({length:5},(_,i)=><Card key={i} className="h-44 animate-pulse"/>):<>{cards.map(([title,value,text,Icon,tone,surface,pct])=><Card key={title} className="h-44 overflow-hidden border-slate-200/90 bg-white shadow-sm"><CardContent className="flex h-full flex-col px-5 pb-4 pt-3.5"><div className="flex items-start justify-between gap-3"><p className="-mt-0.5 truncate text-sm font-semibold tracking-tight text-slate-600">{title}</p><div className={"-mt-0.5 rounded-xl p-2.5 "+surface+" "+tone}><Icon className="h-5 w-5"/></div></div><p className="mt-3 text-3xl font-bold leading-none">{value}</p><div className="mt-auto flex gap-2"><span className={"rounded-full px-2 py-0.5 text-[11px] font-semibold "+surface+" "+tone}>{pct}%</span><span className="truncate text-xs text-slate-500">{text}</span></div></CardContent></Card>)}<Health total={n(s?.totalItems)} data={[{name:"Available",value:n(s?.available),color:C.available},{name:"In Use",value:n(s?.inUse),color:C.inUse},{name:"Damaged / Maintenance",value:n(s?.damagedMaintenance),color:C.damagedMaintenance}]}/></>}</section>
 <section className="grid min-w-0 gap-4 xl:grid-cols-[minmax(0,1.55fr)_minmax(360px,.95fr)]"><Card className="min-w-0 overflow-hidden border-slate-200 shadow-sm"><CardHeader><CardTitle className="text-base">Department Overview</CardTitle></CardHeader><CardContent className="p-0"><div className="h-64 overflow-x-auto"><div className="h-full min-w-[760px]" style={{width:Math.max(620,dept.length*82)}}><ResponsiveContainer width="100%" height="100%"><BarChart data={dept} margin={{left:8,right:12,bottom:24}} onClick={(state:any)=>choose(state?.activePayload?.[0]?.payload?.departmentId)}><XAxis dataKey="departmentName" interval={0} height={54} tick={<DepartmentAxisTick departments={dept} onSelect={choose}/> }/><YAxis allowDecimals={false}/><Tooltip content={<DepartmentTooltip/>}/><Bar dataKey="available" name="Available" stackId="units" barSize={14} onClick={(entry:any,i:number)=>choose(entry?.payload?.departmentId??entry?.departmentId??dept[i]?.departmentId)}>{dept.map((row:any)=><Cell key={row.departmentId} onClick={()=>choose(row.departmentId)} fill={C.available} fillOpacity={!hover||String(row.departmentId)===hover?1:.3} stroke={String(row.departmentId)===hover?"#1d4ed8":"none"} strokeWidth={String(row.departmentId)===hover?3:0}/>)}</Bar><Bar dataKey="inUse" name="In Use" stackId="units" barSize={14} onClick={(entry:any,i:number)=>choose(entry?.payload?.departmentId??entry?.departmentId??dept[i]?.departmentId)}>{dept.map((row:any)=><Cell key={row.departmentId} onClick={()=>choose(row.departmentId)} fill={C.inUse} fillOpacity={!hover||String(row.departmentId)===hover?1:.3} stroke={String(row.departmentId)===hover?"#1d4ed8":"none"} strokeWidth={String(row.departmentId)===hover?3:0}/>)}</Bar><Bar dataKey="damagedMaintenance" name="Damaged / Maintenance" stackId="units" barSize={14} onClick={(entry:any,i:number)=>choose(entry?.payload?.departmentId??entry?.departmentId??dept[i]?.departmentId)}>{dept.map((row:any)=><Cell key={row.departmentId} onClick={()=>choose(row.departmentId)} fill={C.damagedMaintenance} fillOpacity={!hover||String(row.departmentId)===hover?1:.3} stroke={String(row.departmentId)===hover?"#1d4ed8":"none"} strokeWidth={String(row.departmentId)===hover?3:0}/>)}</Bar></BarChart></ResponsiveContainer></div></div></CardContent></Card><Items data={data?.departmentItems} page={filters.departmentItemsPage} setPage={(page:number)=>setFilters(x=>({...x,departmentItemsPage:page}))}/></section><RecentItems items={data?.recentItems??[]}/></div>}
function Health({data,total}:{data:any[];total:number}){return <Card className="flex h-44 min-w-0 flex-col overflow-hidden border-slate-200/90 bg-white shadow-sm"><CardHeader className="px-5 pb-0 pt-3.5"><CardTitle className="text-sm font-semibold tracking-tight text-slate-600">Inventory Health</CardTitle></CardHeader><CardContent className="flex h-[132px] items-center justify-center px-4 pb-3 pt-0">{total?<div className="flex h-28 w-28 items-center justify-center rounded-full bg-slate-50 p-1.5 shadow-inner"><div className="h-[104px] w-[104px]"><ResponsiveContainer><PieChart><Pie data={data} dataKey="value" nameKey="name" innerRadius="62%" outerRadius="90%" paddingAngle={2} stroke="none">{data.map(x=><Cell key={x.name} fill={x.color}/>)}</Pie><Tooltip formatter={(v:any,name:any)=>[String(v)+" items",name]}/></PieChart></ResponsiveContainer></div></div>:<div className="h-24 w-24 rounded-full border-[10px] border-slate-100"/>}</CardContent></Card>}function Items({data,page,setPage}:any){const name=data?.selectedDepartment?.name;return <Card className={"min-w-0 overflow-hidden border-2 shadow-sm "+(name?"border-blue-400 ring-2 ring-blue-100":"border-slate-200")}><CardHeader className="pb-3"><CardTitle className="text-base">{name?"Store Items - "+name:"Store Items"}</CardTitle><p className={"text-xs font-medium "+(name?"text-blue-700":"text-slate-500")}>{name?"Selected department":"Click a department in the chart to view its items."}</p></CardHeader><CardContent className="px-4 pb-4 pt-0"><div className="max-h-[240px] overflow-auto"><table className="w-full min-w-[520px] table-fixed text-xs"><thead className="sticky top-0 bg-white text-left text-slate-500"><tr>{["ID","Item","Avail.","In use","Damaged","Total"].map(x=><th key={x} className="border-b px-2 py-2">{x}</th>)}</tr></thead><tbody>{(data?.items??[]).map((x:any)=><tr key={x.itemId} className="border-b"><td className="px-2 py-2.5 font-mono text-slate-500">{x.itemCode}</td><td className="max-w-32 truncate px-2 py-2.5 font-medium">{x.itemName}</td><td className="px-2 text-green-700">{n(x.available)}</td><td className="px-2 text-amber-700">{n(x.inUse)}</td><td className="px-2 text-red-700">{n(x.damagedMaintenance)}</td><td className="px-2 font-semibold text-slate-700">{n(x.total)}</td></tr>)}</tbody></table></div>{!(data?.items?.length)&&<p className="py-8 text-center text-sm text-slate-500">No item records match this department.</p>}{n(data?.pagination?.totalPages)>1&&<div className="mt-3 flex items-center justify-end gap-2"><Button variant="outline" size="icon" disabled={page<=1} onClick={()=>setPage(page-1)}><ChevronLeft className="h-4 w-4"/></Button><span className="text-xs text-slate-500">Page {data?.pagination?.page??1} of {data?.pagination?.totalPages??0}</span><Button variant="outline" size="icon" disabled={page>=n(data?.pagination?.totalPages)} onClick={()=>setPage(page+1)}><ChevronRight className="h-4 w-4"/></Button></div>}</CardContent></Card>}function DepartmentAxisTick({x,y,payload,index,departments,onSelect}:any){const full=String(payload?.value??'');const label=full.length>10?full.slice(0,10)+'...':full;const departmentId=departments?.find((department:any)=>department.departmentName===full)?.departmentId??departments?.[index]?.departmentId;return <text x={x} y={y} dy={16} textAnchor="middle" fill="#64748b" fontSize={11} className="cursor-pointer" onClick={()=>onSelect(departmentId)}><title>{full}</title>{label}</text>}function DepartmentTooltip({active,payload,label}:any){if(!active||!payload?.length)return null;const rows=payload.filter((entry:any)=>entry.value!==undefined);return <div className="max-w-48 rounded-md border border-slate-200 bg-white px-3 py-2 text-xs shadow-md"><p className="mb-1 truncate font-semibold text-slate-800" title={String(label)}>{label}</p>{rows.map((entry:any)=><p key={entry.name} className="flex justify-between gap-4 py-0.5 text-slate-600"><span>{entry.name}</span><strong className="text-slate-900">{n(entry.value)}</strong></p>)}</div>}function RecentItems({items}:{items:any[]}){return <Card className="overflow-hidden border-slate-200 shadow-sm"><CardHeader className="flex-row items-center justify-between"><CardTitle className="text-base">Recently Added Items</CardTitle><a href="/inventory" className="text-sm font-semibold text-blue-700 hover:underline">View All</a></CardHeader><CardContent className="p-0"><div className="overflow-x-auto"><table className="w-full min-w-[640px] text-sm"><thead className="border-y bg-slate-50 text-left text-xs text-slate-500"><tr>{["Item ID","Item Name","Image","Quantity","Date Added"].map(heading=><th key={heading} className="px-4 py-3 font-medium">{heading}</th>)}</tr></thead><tbody>{items.map(item=><tr key={item.itemId} className="border-b last:border-0"><td className="px-4 py-3 font-mono text-xs text-slate-600">{item.itemCode}</td><td className="px-4 py-3 font-medium">{item.itemName}</td><td className="px-4 py-2"><img className="h-10 w-12 rounded-md border border-slate-100 object-cover" src={item.imageUrl?API+item.imageUrl:"/favicon.svg"} alt="" onError={event=>{event.currentTarget.src="/favicon.svg"}}/></td><td className="px-4 py-3 font-medium">{n(item.quantity)}</td><td className="px-4 py-3 text-slate-600">{item.createdAt?new Date(item.createdAt).toLocaleDateString():"-"}</td></tr>)}</tbody></table></div>{!items.length&&<p className="py-8 text-center text-sm text-slate-500">No recently added items yet.</p>}</CardContent></Card>}